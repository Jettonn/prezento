/**
 * Single source of truth for app branding strings used across pages.
 * Update this once per project — the landing page, sign-in page, and
 * /app dashboard all read from here.
 */
export const APP_NAME = "App";
export const APP_TAGLINE = "A starter template for Nuxt 4 + Better Auth + Drizzle.";
